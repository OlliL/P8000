     RC(MUTOS)                    DABA16                     RC(MUTOS)

     NAME
          rc - REPORT-Compiler

     UEBERSICHT
          rc {[ flags ] file}

     BESCHREIBUNG

          Der Report-Compiler rc uebersetzt ein oder  mehrere  Report-
          Quell-Files   in   Report-Code-Files   (uebersetzte  Report-
          Anweisungen).  Ein Report-Code-File wird zur  Steuerung  der
          Report-Generierung mit rg verwendet.

          Das Quell-File  enthaelt  Report-Anweisungen  und  wahlweise
          Domaenen-Beschreibungen  (z.B.  durch rd (rd(mutos)) mit dem
          Flag  -r  erstellt).   Ist  letzteres  der  Fall,  wird  ein
          Domaenen-Beschreibungs-File erzeugt.

          Flags:

          -c Nur Uebersetzen, keine Codeerzeugung
          -i Eingabe von Standard-Input
          -l automatische Umwandlung der Klein- in Grossbuchstaben
          -o Ausgabe des Report-Code-Files r_code
          -s Unterdrueckung des Uebersetzungsprotokolls
          -v Ausgabe des Uebersetzungsprotokolls

     BEISPIELE
          /* Uebersetze das Quell-File "pers_r" und
             erzeuge das Report-Code-File "r_code" */
             rc -o pers_r

     SIEHE AUCH
          rp_desc(mutos),   rg(mutos),    rd(mutos),    report(mutos),
          report(daba)


     DIAGNOSE
          Im Laufzeitprotokoll (auf  Standard-Output)  werden  selbst-
          erklaerende Hinweise gegeben.
