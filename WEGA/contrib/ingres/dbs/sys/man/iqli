     IQLI(DABA)                   DABA16                    IQLI(DABA)

     NAME
          IQLI - Interaktives QL-Interface von DABA

     BESCHREIBUNG
          Das interaktive QL-Interface (IQL) von  DABA  ist  nach  dem
          Aufruf von iql (iql(mutos)) verfuegbar.

          In  IQL  stehen  die  Datenbanksprache  QL,  die   Terminal-
          Monitor-Kommando-Unterstuetzung und die Makro-Unterstuetzung
          bereit.

          Die Sprache QL wird durch das  DBBS,  die  Terminal-Monitor-
          Kommandos  und  die Makros werden durch den Terminal-Monitor
          realisiert.

          Fuer eine Einarbeitung ist zu empfehlen, sich zuerst mit den
          Terminal-Monitor-Kommandos und der QL-Sprache zu beschaefti-
          gen.





     SIEHE AUCH
          ql(iql), monitor(iql), macros(iql), iql(mutos)
