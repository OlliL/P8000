     CREATER(MUTOS)               DABA16                CREATER(MUTOS)

     NAME
          creater - Erstellen einer neuen Relation

     UEBERSICHT
          creater [ flags ] dbname relname { domname format }

     BESCHREIBUNG
          Die Aussagen zu create(ql) gelten auch fuer creater(mutos).

          Sie sind jedoch sie um Folgendes zu ergaenzen:

          Flags:

          -uname erlaubt es, sich als Nutzer mit dem Login-Namen
               auszugeben; siehe iql(mutos).

          +/-w Warten oder kein Warten auf die Nutzbarkeit der
               Datenbank dbname siehe iql(mutos).
