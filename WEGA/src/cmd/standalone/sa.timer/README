#### bauen von sa.timer

make clean
make sa.timer
mv /sa.timer /sa.timer.orig
mv sa.timer /sa.timer


#### Kernel Modul bauen

	# altes Zeug weg
make clean

	# Objekte bauen
scc -c -O timer.c gmtime.c timegm.c u130.c rtc72421.c 

	# LIB1 sichern
cp /usr/sys/sys/LIB1 /usr/sys/sys/LIB.orig

	# Objekte ins Kernel-Archiv legen
ar rv /usr/sys/sys/LIB1 timer.o rtc72421.o u130.o gmtime.o timegm.o

	# Kernel generieren
cd /usr/sys/conf ; make -f make.wega wega
chmod 400 wega ; mv wega /wega.new

